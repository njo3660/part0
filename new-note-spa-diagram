```mermaid
  sequenceDiagram
    participant browser
    participant server

    note right of browser: When submit is clicked the browser renders the user input on the page then sends the following POST request to the server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa

    note left of server: The data sent via this post is JSON containing the user input and the date
```